# План повышения кибербезопасности — НИКА (nika-v2)

Основано на аудите от 2026-06-19. Ключи на локальной машине (`.env`, `bot/.env`) намеренно исключены из этого плана — их хранение там является ожидаемым поведением для разработки.

Приоритеты: **P0** — закрыть до следующего деплоя, **P1** — на этой неделе, **P2** — в течение месяца, **P3** — backlog.

> **Прогресс (2026-06-19):**
> - ✅ **`/api/chat` захардён** — добавлены cap на payload (≤50 реплик, ≤4000 симв./реплика, ≤24000 суммарно), rate limit 20 req/мин по `user.id` (миграция `007_rate_limits.sql` + `lib/rate-limit.ts`), defense-in-depth `user_id` в `updateConversation`, обработка 429 на клиенте. **Требуется применить миграцию 007 к Supabase.**
> - ⏸️ **Telegram-бот** — отложен по решению: прямой публичной ссылки на бота нет (security-by-obscurity, временно приемлемо). Остаётся каналом без аутентификации — вернуться при публикации.
> - ⏸️ **`/api/auth/register`** — оставлен как есть по решению. Важно: это **мёртвый код** (реальная регистрация идёт через клиентский `supabase.auth.signUp()`); публичный неиспользуемый эндпоинт на service-role ключе. Рекомендация на будущее — удалить.
> - ✅ **Политика паролей (P1 #7)** — сервер Supabase теперь требует min length 8 (было 6) через `scripts/apply-auth-password-policy.mjs`; клиентский маппер ошибок различает утёкший/короткий/несоответствующий пароль. ⏳ HIBP (защита от утёкших) ждёт плана Pro.
> - ⏸️ **`FORCE_PRO_FOR_ALL=true`** — оставлен (нужен для тестов). На будущее: заменить глобальный флаг на allowlist по `user_id`.

---

## P0 — Защита от финансового/ресурсного abuse

Главный риск проекта: открытая регистрация + отключённые лимиты + отсутствие rate limiting = любой может бесплатно и бесконечно жечь бюджет Anthropic API и часы Railway.

1. **Вернуть лимиты.** `lib/subscription.ts` → `FORCE_PRO_FOR_ALL = false`. Если флаг нужен временно для тестов — завести отдельный механизм (allowlist по user_id), а не глобальный выключатель.
2. **Rate limiting на `/api/chat`.** Лимит по `user.id` (например, через Upstash Redis с `@upstash/ratelimit`, благо Railway хорошо дружит с внешним Redis) — N запросов в минуту/час сверх дневного лимита `FREE_DAILY_LIMIT`. Возвращать 429 с понятным сообщением.
3. **Rate limiting на `/api/auth/register`.** Лимит по IP (например, 5 регистраций/час с одного IP) + капча (Cloudflare Turnstile — бесплатный, легко встраивается в Next.js) перед вызовом `admin.createUser`.
4. **Лимит для Telegram-бота.** В `bot/src/bot.ts` добавить per-user rate limit (простая in-memory карта `userId → timestamps` или `grammy` middleware `@grammyjs/ratelimiter`), иначе бот — второй открытый канал для того же abuse.
5. **Алерты по тратам.** Включить budget-алерты в Anthropic Console (уведомление при превышении дневного/месячного порога) — это backstop, если лимиты в коде где-то не сработают.

## P1 — Аутентификация и API-эндпоинты

6. **Убрать раскрытие деталей ошибок** в `/api/auth/register`: не возвращать `error.message` от Supabase клиенту напрямую (позволяет user enumeration — узнать, какие e-mail уже зарегистрированы). Замаппить на generic-сообщение, детали — только в `console.error`.
7. **Политика паролей** при регистрации (минимальная длина, проверка на топ-1000 паролей) — Supabase это умеет на уровне проекта (Auth → Policies), включить там же.
8. **Проверить `SameSite` у Supabase cookie** (должно быть `Lax` или `Strict`) — это единственная защита `/api/chat` от CSRF, так как явного CSRF-токена нет. Если по какой-то причине стоит `None` — добавить CSRF-токен или кастомный заголовок, который проверяется на сервере.
9. **CORS.** Явно проверить/задать `Access-Control-Allow-Origin` для API-роутов, чтобы исключить вызовы с посторонних доменов через серверные интеграции (браузерный same-origin fetch это не требует, но лучше зафиксировать явно, а не полагаться на дефолт).

## P2 — Зависимости и конфигурация сборки

10. **Обновить Next.js** с 14.2.35 до последнего патча 14.2.x (или мигрировать на 15.x) — `npm audit` показывает high/moderate advisories (DoS через Image Optimizer, request smuggling в rewrites, кеш-poisoning Server Components). Сделать `npm audit fix` после обновления и прогнать regression-тест критичных страниц.
11. **Обновить esbuild** в `bot/` (`npm audit fix`) — low-severity дыра в dev-сервере, но обновление бесплатное и без breaking changes.
12. **Включить обратно проверки при сборке.** В `next.config.mjs` сейчас `typescript.ignoreBuildErrors: true` и `eslint.ignoreDuringBuilds: true` — это маскирует баги, в т.ч. потенциально опасные. Цель: включить их хотя бы в CI-пайплайне (отдельный `npm run build:strict`), не блокируя текущий деплой, и постепенно разгребать долг.
13. **Зафиксировать версии зависимостей** (`^` → точные версии или `npm shrinkwrap`) для предсказуемых деплоев на Railway — сейчас минорные обновления могут протекать в прод без проверки.

## P3 — Процессы и мониторинг

14. **Секрет-сканирование репозитория.** Подключить GitHub secret scanning (для публичного репо `Hulu404/nika-v2` он уже частично включён GitHub-ом по умолчанию, но стоит добавить pre-commit hook, например `gitleaks`, чтобы ловить секреты до пуша, а не после).
15. **Логирование без утечек.** Проверить все `console.error` в API-роутах (`api/chat`, `api/auth/register`, `auth/callback`) — убедиться, что туда не попадают токены/ключи целиком (сейчас логируется только `.message`, это нормально, но держать в фокусе при будущих изменениях).
16. **Ротация секретов по графику.** Завести правило: ключи (`ANTHROPIC_API_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `BOT_TOKEN`) меняются раз в N месяцев или сразу после любой возможной утечки (например, после показа в чате/скриншоте/логе).
17. **Healthcheck и алерты Railway.** Настроить уведомления Railway о превышении лимитов плана (CPU/память/часы), чтобы узнать о DoS-атаке раньше, чем приложение просто упадёт.
18. **RLS — держать в порядке.** Текущие политики в `002_rls.sql` корректны; при добавлении новых таблиц — сразу писать RLS-политику в той же миграции, не откладывать на потом.

---

## Чек-лист быстрого старта (минимум для следующего деплоя)

- [ ] `FORCE_PRO_FOR_ALL = false`
- [ ] Rate limit на `/api/chat` и `/api/auth/register`
- [ ] Капча на регистрации
- [ ] Generic-ошибки вместо `error.message` в register
- [ ] Budget-алерт в Anthropic Console
- [ ] `npm audit fix` (next + bot)
